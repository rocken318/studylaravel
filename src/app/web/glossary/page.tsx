import { WebGlossary } from "@/components/WebGlossary";

export const metadata = {
  title: "HTML/CSS 用語集 — 初めてのHTML/CSS",
  description:
    "初めてのHTML/CSSで登場するHTML・CSSの用語を、意味と実務での使い方つきでまとめた用語集。",
};

export default function WebGlossaryPage() {
  return <WebGlossary />;
}
