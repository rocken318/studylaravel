import { WebHome } from "@/components/WebHome";

export const metadata = {
  title: "初めてのHTML/CSS — Webページの土台",
  description:
    "HTML(骨組み)・CSS(見た目)・JS(動き)の三層から、レイアウトとレスポンシブまで。AIが出したコードを読めて直せるようになる4日間の学習コース。",
};

export default function WebCoursePage() {
  return <WebHome />;
}
