import { WebCheatsheet } from "@/components/WebCheatsheet";

export const metadata = {
  title: "HTML/CSS 早見表 — 初めてのHTML/CSS",
  description:
    "HTMLのタグとCSSのプロパティを、意味と使用例つきで素早く引ける早見表(リファレンス)。暗記ではなく「調べる」ためのもの。",
};

export default function WebCheatsheetPage() {
  return <WebCheatsheet />;
}
